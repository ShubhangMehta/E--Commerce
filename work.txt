from django.core.mail import send_mail
from django.conf import settings

send_mail(
    subject="Test Email",
    message="Backup system email test",
    from_email=settings.DEFAULT_FROM_EMAIL,
    recipient_list=[settings.],
    fail_silently=False,
)